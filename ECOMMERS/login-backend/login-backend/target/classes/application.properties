spring.application.name=login
# Base de datos PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/ecommers
spring.datasource.username=postgres
spring.datasource.password=apertura15

# Configuración Hibernate (gestión base de datos)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=public

# Seguridad JWT
jwt.secret=0123456789ABCDEFGHIJKLMNOPQRSTUV
jwt.expirationMs=86400000

# ? Parámetros para el admin
app.admin.username=pablo
app.admin.password=123456
